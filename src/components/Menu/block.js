import * as React from 'react';
import Box from '@mui/material/Box';

export default function MenuBlock() {
    return <Box sx={{width:'250px', display: { xs: 'none', md: 'block' },position:'relative'}}/>
}